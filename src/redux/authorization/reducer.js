const INITIAL_STATE = {
    email: "",
    password: "",
};

export const authorization = (state = INITIAL_STATE, action) => {
    switch (action.type){

        default:
            return state;
    }
};